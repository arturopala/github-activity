{
     "homepage": "https://arturopala.github.io/ddcw-hackathon",
     "devDependencies": {
          "create-elm-app": "^1.10.3",
          "gh-pages": "^1.1.0",
          "http-server": "^0.11.1"
     },
     "scripts": {
          "elm-app": "elm-app",
          "deploy": "elm-app build && gh-pages -d build",
          "start": "elm-app start",
          "build": "elm-app build",
          "test": "elm-app test",
          "format": "elm-format src/ --yes"
     }
}
